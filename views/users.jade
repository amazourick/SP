extends layout

block content
  h1= title
  p Welcome to #{title}
  p Users:
  ul
    each user in user_list
      li
        a(href='/users/#{user.id}/update') #{user.username} : #{user.permission} : #{user.email}
